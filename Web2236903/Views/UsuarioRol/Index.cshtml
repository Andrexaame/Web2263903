@model IEnumerable<Web2236903.Models.usuariorol>

@{
    ViewBag.Title = "Index";
}

<h2>Lista Usuario Rol</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Usuario
        </th>
        <th>
            Rol
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Web2236903.Controllers.UsuarioRolController.NombreRol(item.idRol)
            </td>
            <td>
                @Web2236903.Controllers.UsuarioRolController.NombreUsuario(item.idUsuario)
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { id = item.id }) |
                @Html.ActionLink("Detalles", "Details", new { id = item.id }) |
                @Html.ActionLink("Eliminar", "Delete", new { id = item.id }, new { @class = "listDelete" })
            </td>
        </tr>
    }

</table>

@section scripts{
    <script>
        $(".listDelete").click(function (event) {
            if (!confirm(" ¿Esta seguro de eliminar?")) {
                event.preventDefault();
            }
        })
    </script>
}
