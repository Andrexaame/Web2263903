@model IEnumerable<Web2236903.Models.producto_imagen>

@{
    ViewBag.Title = "Index";
}

<h2>Lista Producto Imagen</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Imagen
        </th>
        <th>
            Producto
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.imagen)
            </td>
            <td>
                @Web2236903.Controllers.ProductoImagenController.NombreProducto(item.id_producto)
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { id = item.id }) |
                @Html.ActionLink("Detalles", "Details", new { id = item.id }) |
                @Html.ActionLink("Eliminar", "Delete", new { id = item.id }, new { @class = "listDelete" })
            </td>
        </tr>
    }

</table>

@section scripts{
    <script>
        $(".listDelete").click(function (event) {
            if (!confirm(" ¿Esta seguro de eliminar?")) {
                event.preventDefault();
            }
        })
    </script>
}
